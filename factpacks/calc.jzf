name calc
author Alexander Boyd
description Adds a "calc" command that can be used to calculate the answer to a mathematical 
  equation. For example, with this factpack installed, you could run "~calc (2+3)*4", and 
  the bot would send back "20".
>calc {catch|{eval|%1-%}|e|That's an invalid arithmetic equation: %e-root-message%}