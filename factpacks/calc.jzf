name=local.misc.calc
author=Alexander Boyd
scope=any
description=Adds a "calc" command that can be used to calculate the answer to a mathematical \
equation. For example, with this factpack installed, you could run "~calc (2+3)*4", and the \
bot would send back "20".
t.calc={{eval||%1-%}}