name local.misc.bf
author Alexander Boyd
description Adds a BF interpreter. ~bf <code> <input> is used to run a program. The
  BF interpreter implemented by this factpack is extremely inefficient, but it works,
  which is sufficient to prove that Fact is turing-complete.
>bf
