package org.opengroove.jzbot;

/**
 * A plugin for JZBot. Plugins can register protocols, add commands, listen for
 * extended events, kick, ban, send messages, join servers, leave servers,
 * set/remove operator status, and such.
 * 
 * @author Alexander Boyd
 * 
 */
public interface Plugin
{
    
}
