package jw.jzbot.psystem.client;

/**
 * An interface defining all of the RPC methods that a plugin can call. JZBot itself
 * implements these methods, and allows them to be called over RPC.
 * 
 * @author Alexander Boyd
 * 
 */
public interface PluginClientInterface
{
    
}
