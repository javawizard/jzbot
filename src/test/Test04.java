package test;

public class Test04
{
    
    /**
     * @param args
     */
    public static void main(String[] args)
    {
        System.out
                .println(("Use \"~config <varname>\" to see a var (value and description) " +
                        "or \"~config "
                        + "<varname> <value>\" to set a var. Currently, "
                        + "allowed varnames are, separated by spaces: ")
                        .length());
    }
}
